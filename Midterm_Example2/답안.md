메모리 해제 체크리스트 if (A) A->Release();

카테고리	대상 객체 (반드시 Release 필요)
인프라	Device, DeviceContext, SwapChain
쉐이더	VertexShader, PixelShader, InputLayout
리소스	VertexBuffer, IndexBuffer, ConstantBuffer
뷰(View)	RenderTargetView
임시 메모리	ID3DBlob (이건 보통 생성 직후 중간에 해제함!)
